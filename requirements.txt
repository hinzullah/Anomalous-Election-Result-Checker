# Ultra-minimal version for deployment
# Use this if the full version causes build issues

# Core Flask
Flask==3.1.3
gunicorn==21.2.0

# Lighter ML option - choose ONE:
# Option A: TensorFlow (larger, ~500MB)
tensorflow-cpu==2.21.0  # CPU-only version (smaller)

# Option B: scikit-learn (smaller, ~50MB)
# scikit-learn==1.7.2
# joblib==1.5.3

# Image Processing
opencv-python-headless==4.12.0.88  # Headless = no GUI dependencies
Pillow==12.1.1

# PDF Processing  
pypdf==6.7.0

# Essential utilities
numpy==2.2.6